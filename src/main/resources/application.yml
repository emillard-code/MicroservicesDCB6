server:
  port: 9006

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/emillard-code/MicroservicesDCBConfiguration
          clone-on-start: true